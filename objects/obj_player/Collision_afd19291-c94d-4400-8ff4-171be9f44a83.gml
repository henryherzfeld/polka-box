/// @description Show Chest
with (other) {
    // Check if we can open the chest
	if (canOpen == true) {
		//Chest is close, open it!
		if (show_chest == false) {
		    //Open Chest
			show_chest = true;
			}
	}
}
