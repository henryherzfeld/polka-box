/// @description Insert description here

event_map = ds_map_create();

enum event {
	pause,
	new_evidence,
	talk,
	talk_weeraway,
	find_poppy,
	test,
	length
}

enum action {
	talk,
	length,
}